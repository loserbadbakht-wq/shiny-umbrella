name = "shiny-link"
main = "src/index.js"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]

# Vars are injected by the GitHub Actions workflow via --var
# BOT_TOKEN and DB_ENCRYPTION_KEY
